home_publications:
    path: /home
    defaults: { _controller: AppBundle:Publication:index }
    
stats_publication:
    path: /publication/stats/{id}
    defaults: { _controller: AppBundle:Publication:statsPublication, id:null }
    
    
remove_publication:
    path: /publication/remove/{id}
    defaults: { _controller: AppBundle:Publication:removePublication, id:null}
